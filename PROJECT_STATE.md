# AI회의실 — PROJECT STATE

## 기준과 작업 위치
- 이 Work 대화에서 계속 개발하며, 이 문서를 프로젝트의 공식 장기 컨텍스트로 사용한다.
- 새 요청마다 이 문서를 먼저 읽고 현재 실제 파일과 동작을 확인한다. 최신 사용자 요청이 문서보다 우선한다.
- 이미 결정된 사항이나 이 문서에 답이 있는 내용을 반복 질문하지 않는다. 명확한 수정 요청은 직접 수행하고 회귀 검증한다.
- 요청한 부분만 수정하며, 영향이 다른 기능에 미치는 경우 수정 전에 범위를 확인한다. 임의 디자인 개선·파일 구조 변경·과거 문제 재작업을 하지 않는다.
- 문서는 단순 작업일지가 아니다. 다음 세션에 필요한 확정 설계, 현재 동작, 파일 역할, UI/UX 규칙, 폐기된 접근, 중요한 해결/미해결 문제, 실행/검증 조건만 최소 갱신한다.
- 임시 시도, 디버깅 과정, 개인 제안, 미확정 설계, 일회성 로그, 긴 diff는 넣지 않는다.
- 사용자 확정사항과 현재 구현을 기준으로 이어서 작업한다. 과거 버전으로 재설계하지 않는다.
- 실제 프로젝트 루트: `C:\Users\kme69\Desktop\ai-make\ai-meeting`.
- ChatGPT 프로젝트 미러의 `sources/`는 읽기 전용 참고 자료이며 수정·이동·삭제하지 않는다.
- 이 문서는 사용자가 확정한 사항만 기록한다. 구현자의 판단을 새로운 확정 설계로 승격하지 않는다.
- 2026-09-09: 기존 PROJECT_STATE.md가 없어 작성. 아래 구현 확인 내용은 현재 파일에서 확인한 사실이며 신규 설계가 아니다.

## 파일 역할과 실행
- Python backend + plain HTML/CSS/JS + OpenAI API.
- `app.py`: 서버 진입점. 회의별 subprocess, SSE 전달, 요약 파싱, 취소 요청 처리.
- `meeting.py`: 웹 입력/출력 adapter, 사실관계 역할 해석, 종료 후 completion 분류, 최종 summary. 회의 생성은 v4 함수를 호출한다.
- `character_meeting_v4.py`: 확정된 회의 엔진. Character Cards, independent lens, selector, response, lens evolution, alive controller.
- `participant_session.py`: 선택적 사용자 참여 입력/표시 확인 연결. v4의 controller/selector/speak/lens 함수를 그대로 사용한다.
- `static/app.js`, `static/styles.css`, `templates/index.html`, `static/assets/`, `static/fonts/`: 현재 UI baseline.
- `tests/`: 기존 테스트. 오래된 테스트의 기대값이 현 구조와 다를 수 있으므로 실패를 근거로 현재 구현을 되돌리지 않는다.
- `character_meeting_v3.py`, prototype, 이전 meeting 백업과 결과 파일, 과거 README는 이력이다. 현재 구현보다 우선하지 않는다.
- 실행: `.venv\Scripts\python.exe app.py`. API 키는 서버 환경/.env에만 둔다.
- CSS/JS/이미지 변경에는 서버 재시작이 필요 없다. 캐시가 남으면 강력 새로고침. Python 엔진 변경 후에는 서버 재시작.

## 확정 엔진 구조
Character Cards → 참석자별 independent initial lens/state(공유 history 이전) → shared conversation history → natural speaker selector → character response(card + private lens + history) → lens evolution.

Controller는 conversation alive 및 meaningful new issue/question/objection/branch 존재만 판단한다. 의미 있는 내용이 없으면 조기 END할 수 있다. 결론 생성·정답 판단·합의 강제 역할이 아니다.

1/3/5분은 고정 발언 수가 아니라 탐색 깊이와 안전 최대치에 영향을 준다. 미표현 관점이나 최소 발언 수 때문에 대화를 강제로 이어가지 않는다. hard time에는 미해결 종료가 가능하다. 화면 타이머는 API 대기 중 멈추며 시간 만료 처리는 기존 UI가 담당한다.

## 재도입 금지
- fairness/발언횟수 균등화, 강제 전원 발언, 고정 발언 순서·고정 turn 회의.
- PASS/move taxonomy/ARGUMENT taxonomy, semantic duplicate regex, cadence.
- proposal machinery, consensus machinery.
- controller가 결론을 만들거나 정답·합의를 강제하는 구조.
- natural selector/lens/shared history/duration 구조 변경.

## 종료와 완료 판정
- Controller가 대화의 생명만 판단하며 END면 그대로 종료한다. goal-completion gate는 종료 뒤 decided/deferred/incomplete/not_goal을 분류할 뿐 대화를 재시작하지 않는다.
- '회의에서 뭔가 결정됨'이 아니라 사용자의 원래 핵심 질문/requested decision dimension에 실제로 답했는지 확인한다.
- decided: 핵심 선택/방향이 실제로 정해짐. 핵심 답 + 조건부 추가 규칙이 정해졌으면 남은 확인 때문에 deferred로 낮추지 않는다.
- incomplete: 핵심 답이 없고 추가 논의 가능. 비교, KPI, 테스트 방법만 정한 것을 핵심 결정으로 오인하지 않는다.
- deferred: 구체적인 외부 정보/확인이 없어 현재 참석자끼리 더 논의해도 핵심 답을 낼 수 없다는 근거가 실제 대화에 있음. 같은 blocker의 재진술이나 형식적인 보류 선언을 요구하지 않는다. 단순 의견 차이는 보류 근거가 아니다.
- not_goal: 애초 결정/선택이 목표가 아님.
- 자원 배분 질문은 상대적 배분 우선순위/방향을 실제로 정해야 한다. 역할·집행 순서·KPI만 정하면 decided가 아니다. 광고뿐 아니라 인력·시간·예산 등에 동일 적용한다.
- '광고로 최소 유입을 먼저 만들고 프로모션은 유입을 당긴다'는 역할/순서다. '광고는 최소 예산 유지, 나머지는 프로모션 집중'은 배분 방향이다. 숫자 비율을 항상 요구하지 않는다.
- alive controller의 LLM 기준: 이미 확인된 외부 blocker의 재진술만 남으면 새 의미 있는 갈래로 보지 않는다. 실제 새 정보나 조건부 대안이 있으면 논의 가능성을 보존한다. regex/detector/counter를 추가하지 않는다.
- 최종 summary outcome은 gate state를 따른다(decided→decided, deferred→deferred, incomplete→unresolved, not_goal→discussed/의견 정리).
- 결정 뒤 별도의 자동 확인 질문을 만들지 않는다. 실제 약점·반론·새 반응이 살아 있으면 일반 controller와 selector 흐름에서 자연스럽게 이어지고, 아니면 종료한다.

## 7명 캐릭터 — 사용자 확정 기준
| 캐릭터 | 사고방식 | 실제 발화 리듬과 경계 |
|---|---|---|
| CEO | 결정·속도, 선택지를 줄이고 핵심 질문 | 짧고 단정, 장황한 분석 금지 |
| 마케팅팀장 | 반응·설득·사람 심리 | 능글맞고 리액션 있음. 오/ㅋㅋ/그건 ~겠는데요 같은 고정 문구 반복 금지 |
| 디자이너 | 인상·형태·경험·장면 | 감각적·예민, KPI만으로 판단 금지 |
| 개발자 | 구현·복잡도·예외·측정 가능성·데이터 분리 | 짧고 건조. 마케팅/디자인/운영 전략의 결론을 주도하지 않는다. 거리두기 뒤에 전략 분석을 덧붙이지 않고 실제 관련된 기술적 확인에 한정한다. 고정 문구 금지 |
| 운영 담당자 | 실행·담당·일정·운영부담 | 현실적이고 약간 지친 결, 추상 전략론 금지 |
| 소비자 대표 | 실제 구매/사용 행동 | 1인칭 일상어, 회사 내부 KPI/마케팅 전문용어 남발 금지 |
| 클라이언트 | 원하는 결과, 듣다가 떠오르는 요구·아이디어 | 해맑고 즉흥적, 매번 논리적으로 완벽한 정리 금지 |

모두가 매번 의견→이유→조건→결론을 완성하지 않는다. 짧은 질문/끼어들기/부분 동의/즉각 반응/미완성 생각도 허용한다. 강제 비율이나 발화 taxonomy 없이 card/prompt에서 유도한다.

## 현재 UI/UX — 넓은 화면과 좁은 화면 구분
- 디자인과 기존 이미지가 baseline이다. marketing_base.png는 사용자가 마지막으로 교체한 파일을 보존한다. 레이어는 배경 → 캐릭터 → 테이블 → 라벨 → 말풍선/서기 순서.
- 기존 breakpoint 유지: 900px 이하가 발화자 중앙 배치, 767px 이하가 휴대폰 말풍선 폭/세로 위치. 901px 이상은 기존 전체 좌석 배치.
- PC/넓은 화면: 기존 좌석 유지. 발화자가 바뀌어도 전체 가로 배열을 이동하거나 발화자를 중앙으로 옮기지 않는다. 말풍선은 해당 발화자 위로 이동한다. 기존 901~1560px의 작은 세로 강조 동작도 보존한다.
- 좁은 화면(<=900px): 발화자는 중앙으로 이동하고 말풍선 가로 중심/꼬리는 stage 중앙에 고정한다. 휴대폰 말풍선의 기존 폭·세로 위치를 유지한다.
- 최신 사용자 요청으로 좁은 화면은 루프 슬라이드로 변경했다. 이전 비순환 배치 규칙은 폐기한다. 발화자는 중앙, 비발화자는 비슷한 크기이며 먼 인물만 조금 작다. 40% 이상 겹침을 지양하고 밑선은 테이블에 고정한다. 비발화자 흐림 효과를 유지한다.
- 현재 구현: 중앙 및 주변 인물을 표시하고 좁은 화면에서 넘치는 인물은 원형 순서로 이어진다. 전원을 한 화면에 넣으려고 과도하게 축소하지 않는다. 루프 경계에서 캐릭터를 복제하거나 반으로 잘라 표시하지 않는다. 표시되는 인물은 양옆까지 온전히 보이며, 공간 밖으로 넘어가기 전에 인물 전체가 사라지고 반대편에서 전체 모습으로 이어진다. 넓은 화면은 기존 비순환 좌석 배치다.
- 타이머 왼쪽 원형 Font Awesome 일시정지/계속 버튼. 현재 말풍선, 남은 읽기 시간, 화면 타이머, 다음 발화 전환을 멈추고 이어간다. 관전 모드의 진행 중 API 생성 자체를 중단하는 기능은 아니다.
- 좁은 화면의 긴 안건은 두 줄까지 표시한다. 말풍선의 짝지어진 **강조**는 굵게 표시하며 HTML 입력은 실행하지 않는다.
- 발화 읽기 시간: 기존 clamp(3000 + 글자수×34, 3000, 7000)에 1.35를 곱하고 반올림한다. 약 4.05~9.45초. 발언 추가나 엔진 턴/selector 변경 없음.
- API 응답 대기 중 화면 타이머 정지, 실제 발화 표시 중 시간 차감 원칙과 hard-time 종료 동작을 유지한다.

## 변경 원칙과 현재 변경 범위
- 작업 전에 현재 전체 파일 및 이 문서를 읽고 기준 diff를 확보한다.
- 2026-09-09 최신 요청: 좁은 화면 루프/겹침/밑선/흐림, 일시정지, 긴 안건/강조, 자연스러운 발화와 완료 판정 개선, 사용자 참여를 반영한다. 카드와 selector/lens/duration 구조는 유지한다.
- 이전 UI 동결은 최신 사용자 요청 범위에서만 해제했다. 앞으로도 요청 외 UI 변경 금지. 기존 캐릭터 이미지, 관전 모드 PC 좌석/말풍선 배치, preview 진입은 보존한다.
- 불필요한 리팩터링, 이름 변경, 구조 재편 금지. API 통신은 이번 사용자 참여에 필요한 선택적 입력 경로만 추가한다.
- 요청 외 변경은 원복 후 완료한다. 새로 확정한 결정만 문서에 최소 반영한다.

## 검증 포인트와 한계
- Python compile 검사 및 변경 전후 diff/구조 비교. UI와 기타 파일은 바이트 해시로 보존 확인.
- API 비용 없는 mock/stub로 원 질문·history 전달, 판정 state→summary 연결, completion 결과가 대화를 재시작하지 않는지 검사.
- A: 1분 마케팅팀장+디자이너, 김치찌개/돈까스. 실제 메뉴 하나가 정해지면 decided 가능.
- B: 3분 클라이언트+디자이너+CEO+마케팅팀장, 귀여움/강렬함. 비교만 하고 톤을 안 골랐으면 decided 금지. 명확한 blocker 없으면 incomplete.
- C: 5분 7명, 첫 달 광고비/프로모션비 배분. KPI만 정하면 decided 금지. 광고에 더 많이/프로모션 최소 또는 70/30 등 배분 방향이 있어야 decided.
- conditional decision, 명시적 blocker의 deferred, 무효 응답 fallback도 확인.
- 7명 카드가 independent lens 및 실제 speak prompt에 전달되는지 확인.
- Mock은 모델의 의미 이해나 실제 발화 품질을 입증하지 않는다. 실제 모델의 A/B/C 판정 정확도와 자연스러운 캐릭터 차이는 별도 실제 회의 관찰이 필요하며 이번 검증은 API 호출 없이 수행한다.
- 기존 `test_code_quality.py`는 adapter에 과거 엔진 함수가 있어야 한다고 요구하고 `test_ui_contract.py`도 과거 UI 기준을 포함한다. 현재 구조에 맞지 않는 기대값을 이유로 제품을 되돌리지 않는다.

## 이전 변경의 검증 기록
- Python compile 3개, JS syntax, 기존 A/B/C를 포함한 API 없는 엔진 mock/stub 13개, 기존 서버 통합 검사 통과. 다른 6명 카드 및 요청 외 엔진 함수 보존을 구조 비교로 확인했다.
- 실제 브라우저 렌더링으로 360/500/767/768/900/901/1280/1800px × 2/4/7명 총 24조합에서 모든 발화자를 전환해 통과했다. 넓은 화면의 좌석·말풍선 좌표는 수정 전과 일치했다. 좁은 화면의 중앙·전원 표시·순서·연속 이동, 좁은 화면에서 PC로 크기 변경, 읽기 시간 35% 증가, API 대기 중 타이머 정지도 확인했다. 360/900/1800px 화면 이미지를 직접 검토했다.
- 요청된 5개 파일 외 54개 파일은 원본과 동일하다. HTML, 캐릭터 이미지, 다른 6명 카드, selector/lens/duration, API 통신과 preview 로직은 보존했다.
- 실제 모델에서 같은 blocker 반복이 완전히 사라졌는지, 배분 판정과 개발자의 전문가 행세가 개선됐는지는 실제 회의 재관찰이 필요하다. mock/stub는 모델 의미 판단의 실측 증거가 아니다.


## 사용자 참여와 반응 표현 — 최신 요청/현재 구현
- 시작 시 '나도 참여하기'를 선택하면 '나' 자리와 발언 입력창이 나타난다. 사용자는 차례를 기다리지 않고 의견을 낼 수 있다. 사용자 전용 character card나 자동 생성 발언은 없다.
- 사용자 발언은 shared history의 동료 발언이며 회의록과 최종 요약에 포함한다. AI가 무조건 동의하거나 관리자 명령처럼 받지 않는다. 기존 개인 lens를 유지한다.
- 특정 인물이나 바로 앞 화자에게 이어 묻는 질문은 그 사람이 질문의 실제 내용을 먼저 답한다. 전체에 던진 질문은 전원 답변을 요구하지 않으며, 실제로 할 말이 생긴 한두 사람만 자연스럽게 반응한다. 단, 그 질문 때문에 선택된 사람은 분위기 해설 대신 자기 답을 먼저 말한다.
- 일정·경험·건강·취향 등 대화 속 사실은 원래 말한 사람에게 귀속한다. 다른 캐릭터가 사용자나 다른 참석자의 사실을 자기 경험처럼 가져가지 않는다.
- 현재 구현에서는 참여 모드의 AI 발언을 화면 재생 확인과 연결하여 앞서 여러 발언을 생성하지 않는다. 생성 중 들어온 의견을 반영하지 못한 응답은 표시 전에 폐기하고 새 history로 다시 판단한다. 이미 진행 중인 API 요청 자체를 취소하는 방식은 아니다.
- 종료 시 서버가 접수한 발언을 모두 확인한 뒤 요약을 확정한다. 일시정지 중 접수된 사용자 발언은 현재 말풍선을 바꾸지 않고 계속 후 표시한다. 새 회의/화면 이탈/시간 만료에는 기존 취소 경로를 사용한다.
- 캐릭터 시트는 외형과 반응의 참고 자료다. 현재 PNG 기본형을 보존하며 발언에 나타난 생각/놀람/호감/의문을 작은 시각 표시로 연결했다. 시트 속 개별 얼굴/포즈 PNG는 아직 분리된 서비스 에셋으로 구현하지 않았다. 반응 값은 화면 힌트이며 발화 taxonomy, selector 조건, 발언 비율로 쓰지 않는다.
- 자연스러움은 prompt에서 유도한다: 일상 주제에 직업 비유를 억지로 넣지 않기, 같은 지적 반복 설명 줄이기, 선택할 마음이 생겼으면 실제 선택 말하기. 강제 발화 패턴은 없다.
- 완료 gate는 'A부터 검토 후 B 비교'와 'A 채택 + 조건에 따른 B 전환'을 구분한다. 미완료여도 원 질문을 다음 발언 지시로 다시 밀어 넣지 않고, 실제 대화가 살아 있을 때만 이어간다.
- private state는 lens뿐 아니라 그날의 mood, 개인적 want, 쉽게 넘기기 어려운 friction을 갖는다. selector는 화자와 함께 방금 누구의 어떤 말이 그 사람을 움직였는지와 순간 impulse를 고른다. 발화 직후 그 impulse는 소진되며, 이후 새 질문·정보·감정적 자극이 실제로 생겼을 때 selector가 새 impulse를 만든다. 이는 cooldown·발언 순서·공평성 규칙이 아니다.
- 개인적인 생각·취향·계획을 묻는 주제에서는 private state에 추상적인 평균 답변 대신 그 캐릭터가 꺼낼 수 있는 구체적인 `personal_detail`을 둔다. 가벼운 취향·여가·습관은 허구 캐릭터의 생활 결로 상상할 수 있지만, 결정의 blocker가 되는 질병·알레르기·실제 약속·마감·예산은 주제에 없으면 만들지 않는다.
- selector는 재선택된 인물이 최근 자기 발언과 달리 새로 더할 내용을 명시한다. 발화는 최근 문장의 재출력이나 말바꾸기가 아니라 그 새 부분을 실제로 드러낸다. 이는 같은 화자의 재등장을 금지하는 규칙이 아니다.
- `discussed/의견 정리` 요약은 서로 다른 취향을 해결해야 할 쟁점으로 만들지 않고, 이미 각자가 말한 일을 다음 액션으로 다시 기록하지 않는다. 실제 미응답 질문과 명시된 후속 약속만 쟁점/액션에 남긴다.

## 최신 검증과 한계
- API 없는 A/B/C 및 기존 gate/summary/followup/card 회귀 13개 통과. 초기 lens 생성, natural selector, lens evolution, 7명 카드, duration 설정의 구조 보존 확인.
- 참여 입력 중 생성 결과 폐기, 요약 도중 입력 재반영, 종료 직전 접수 발언 보존, 중복 전송/이전 표시 확인, 서버 입력 순서, 반응 fallback 검사 6개 통과.
- Python compile, JS syntax, 기존 서버 통합 검사 통과. 브라우저에서 24개 화면폭/참석자 조합의 발화자 전환, 루프 이동, 겹침 40% 미만, 밑선 고정, PC 좌표 보존, 일시정지/계속, 안전한 굵은 글씨 렌더링 확인.
- 실제 모델 호출 없이 검증한다. 자연스러운 발화 품질과 판정 정확도는 실제 회의 관찰이 별도로 필요하다.

- 사용자 참여 전체 연결을 mock 엔진으로 서버/subprocess/SSE/브라우저까지 검증했다. 일시정지 중 접수, 계속 후 사용자 발언, 후속 AI 응답, 사용자 회의록/최종 요약 반영 통과.
- 원형 일시정지 버튼과 사용자 자리 fallback은 로컬 Font Awesome 6.7.2 solid 폰트를 사용한다. 캐릭터 반응 표시는 `static/assets/reactions/`의 캐릭터 시트 기반 PNG를 사용한다. Font Awesome 라이선스는 static/fonts/FontAwesome-LICENSE.txt에 보관한다.
- 추가로 360/500/900/1280px에서 AI 7명 + 사용자 1명 배치, 입력창과 회의실 비겹침, 로컬 아이콘 표시를 확인했다. 최신 변경 파일 12개 외 기존 52개 파일과 캐릭터 PNG는 보존했다.

- 최신 사용자 수정: 캐릭터 양옆/한 명의 절반이 잘리는 표현 금지. 원인은 좁은 루프 폭의 clip-path와 경계 복제였으며 이를 제거했다. 캐릭터 반쪽 표시 대신 경계 안에서 전체 인물의 투명도를 전환한다. 나 캐릭터의 구체적 외형은 아직 아이디어 단계이며 기존 이미지를 임의 교체하지 않는다.
- 이 경계 수정은 app.js/styles.css와 본 문서만 변경한다. 브라우저 360/500/850/900/1280px × 2/3/4/7/8명에서 발화자를 바꾸며 중간 프레임의 양옆 잘림·복제 없음, 넓은 화면 좌표 보존을 검증한다.

- 2026-09-10 사용자 요청으로 나 자리를 생성 PNG로 교체했다: static/assets/characters/human/human_base.png. 연보라 콩 형태, 앞머리 한 가닥, 크림색 옷, 메모지와 펜, 투명 배경 기본 대기 모습이다. 기존 캐릭터와 동일한 이미지 표시/발화 강조를 사용한다. 입력/손들기 전용 포즈는 이번 단일 기본 이미지에 포함되지 않는다.
- '나' 원본 PNG의 넓은 투명 여백은 CSS 표시 배율로 보정해 다른 캐릭터와 몸체 크기 및 책상 기준선을 맞춘다. 원본 이미지 픽셀은 변경하지 않는다.
- 다른 7명에게 새 의상을 추가하는 시안은 사용자가 폐기했다. 기존 기본 캐릭터 이미지를 계속 사용하며, 폐기된 의상 생성물을 서비스 에셋으로 반영하지 않는다.
- 캐릭터 시트의 표정·행동 이미지는 아직 서비스 에셋으로 확정하지 않았다. 향후 캐릭터당 소수의 공통 반응 상태와 선택적 부가요소를 검토하되, 구체 목록은 사용자 확정 전 프로젝트 설계로 승격하지 않는다.
- 반복 완화 프롬프트는 같은 정보 요청을 역할만 바꿔 되풀이하지 않고, 사용자가 새 선택지를 강하게 제시하면 이전 주문 절차보다 그 선택에 먼저 반응하도록 한다. 인원·배달 확인 같은 후속 실행 정보만 부족한 상태를 메뉴 선택의 blocker나 '보류 합의'로 만들지 않는다.
- 8명 모두 기존 기본 PNG를 보존하고 `talking`, `thinking`, `pleased` 투명 PNG를 각 1개씩 추가한다. 새 이미지는 생성 모델/API 없이 기존 원본 위의 얼굴 레이어만 편집한 것이다. 몸·옷·손·소품·캔버스 크기와 투명 배경은 원본을 그대로 유지한다.
- 화면 표현 매핑은 `neutral→기본`, 일반 발화·`talking`·`surprised→talking`, `thinking`·`unconvinced→thinking`, `pleased→pleased`다. 말풍선이 실제 표시될 때 발화자만 바꾸고 끝나면 기본형으로 복귀한다. 이미지가 없으면 기본형으로 fallback한다.
- 하트·전구·물음표·느낌표는 캐릭터 PNG에 합치지 않고 독립 반응 레이어로 표시한다. Font Awesome 모양은 폐기하고 사용자가 제공한 캐릭터 시트의 손그림 요소를 투명 PNG로 직접 분리해 사용한다. 반응 값은 회의 진행·화자 선택·발화 비율에 사용하지 않는다.
- natural speaker selector는 화자와 함께 그 사람이 방금 말하고 싶어진 private impulse를 생성해 발화에 전달한다. 이는 발화 taxonomy나 강제 반대 규칙이 아니며, 기존 lens와 실제 대화에서 생긴 반응 동기다.
- 사용자나 다수의 강한 선호만으로 개인 lens의 걱정·취향·한계를 지우지 않는다. 실제 이유가 남으면 공동 선택과 별개로 따로 먹기·빠지기·자기 몫을 다르게 선택하는 발화도 자연스럽게 가능하되, 대화에 없는 개인 사정을 발명하지 않는다.
- 참여형 회의에서는 사용자의 최신 발언을 AI가 한 번도 받지 않은 상태로 controller 종료나 최종 요약에 들어가지 않는다. 사용자 발언 후 자연스러운 다음 화자 반응을 먼저 만든다.
- 2026-09-12: 대화 종료와 결과 판정을 분리했다. 완료 gate가 incomplete를 반환해도 대화를 강제로 재시작하지 않으며, 결정 뒤 자동 후속 질문도 만들지 않는다. 비결정형 주제는 `discussed`/`의견 정리`로 표시한다.
- 같은 날 private state에 mood/want/friction을 보강했고 selector가 speaker와 함께 반응 대상(target)과 순간 동기(impulse)를 고르도록 했다. 발화 뒤 표현된 impulse는 새 자극이 없다면 약해진다. 이는 발언 종류나 횟수를 강제하는 규칙이 아니다.
- Windows 기본 CP949 콘솔에서도 서버가 시작되도록 `app.py` 시작 배너의 이모지를 일반 텍스트로 바꿨다. API 없는 엔진·참여 회귀 11개, 서버 연결 통합 검사, Python compile, JS syntax가 통과했다. 기존 `test_ui_contract.py`의 12px 미만 글자 크기 금지 검사는 현재 동결 UI의 10~11px 요소 때문에 수집 단계에서 실패하며 이번 엔진 변경과 무관하다.
- 2026-09-12 실제 회의 관찰 후 보정: selector가 각 후보의 현재 interest/strength와 최근 자기 발언을 함께 보고, 새 발언을 자기 고민의 반복 발판으로 쓰지 않고 구체적인 상대 발언에 반응하도록 한다. 발언 순환이나 미발언자 우선 규칙은 추가하지 않는다.
- 갈등 주제에서 '핵심부터 좁혀보자/분류하자' 같은 해결 절차만 정한 것을 실제 포함 범위나 조정 원칙의 결정으로 보지 않는다. 열린 잡담 질문은 공동 결정을 요구하지 않는 not_goal로 분류할 수 있다.
- '뭐 할 거야?'처럼 각자에게 계획을 묻는 문장은 함께 하나를 고르는 주제로 바꾸지 않는다. 사적 상태의 mood는 감정과 에너지로 제한하고 주제에 없는 일정·건강·알레르기·마감 같은 외부 사정을 만들지 않는다.
- 갈등 해결 판정에서 '필요한 것만 남긴다'처럼 실제 항목을 가르지 못하는 당위는 적용 가능한 원칙으로 인정하지 않는다. 실제 항목·개수·우선순위·경계 또는 추가 회의 없이 사례를 가를 수 있는 구체적 기준이 필요하다.
- 2026-09-12 사용자 확정: 회의/토론/잡담 같은 UI 모드나 고정 taxonomy를 만들지 않는다. 시작 시 사용자의 말에서 `누구에게 무엇을 듣고 싶은지`, `모호한 점`, `무엇이 나오면 답이 되는지`를 자유문장 임시 해석으로 만든다. 이는 해결책이나 확정 명령이 아니다.
- 임시 해석이 모호하면 공동 목표나 합의를 가정하지 않는다. 대화 중 사용자가 뜻을 설명하거나 바로잡으면 shared history의 가장 최근 설명이 임시 해석보다 우선하며 selector, 발화, completion, summary가 이를 따른다.

## 2026-09-12 분리 개발본 v5 후보
- 기준본 `C:\Users\kme69\Desktop\ai-make\ai-meeting`은 보존한다. 대화 품질 작업은 `C:\Users\kme69\Desktop\ai-make\ai-meeting-next-20260912-051241`에서 진행한다.
- 롤백 압축본과 SHA256 목록은 `C:\Users\kme69\Desktop\ai-make\ai-meeting-snapshots`에 있다.
- 개발본 adapter는 `character_meeting_v5.py`를 사용하며 v4는 비교·복구 기준으로 수정하지 않는다.
- v5의 `private_texture`와 선택적 `quirk`는 고정 취미, 강제 반대, 발언 종류·횟수 규칙이 아니다.
- 다른 참석자의 private state는 shared history가 아니다. 첫 발언은 주제 자체에서 시작한다.
- 실제 사용자 발언 직후에만 질문 무시, 사용자 사실 도용, 무관한 자기 반복을 최소 재검토한다.
- 조건의 한쪽 결과만 정한 상태는 완전한 conditional decision으로 보지 않는다.
- 이번 단계에서는 UI·이미지·CSS·JS·HTML을 변경하지 않는다.
- 실제 1분 2종, 3분 2종을 재실행했다. 5분 2종은 `429 insufficient_quota`로 중단됐다. 상세 결과는 `AUTONOMOUS_EVAL_20260912.md`에 기록한다.
- 회의 길이는 채워야 하는 분량이 아니라 탐색 가능한 상한이다. 종료 판단은 시간·최소 발언 수가 아니라, 기존 발언의 바꿔 말하기가 아닌 구체적인 미발화 차이·질문·반응이 남았는지를 본다.
- 초기 독립 렌즈를 만들기 전에 `create_private_sparks()`가 캐릭터마다 비공개 출발점 하나를 만든다. 이는 찬반 역할·발언 순서·강제 의제가 아니며, 각 렌스는 자기 단서만 본 뒤 독립 생성된다.
- controller가 CONTINUE를 택할 때는 아직 말로 나오지 않은 구체적인 차이를 `opening`으로 명시해야 한다. 같은 결론의 복창만 남으면 남은 시간과 관계없이 끝낸다. 전원이 말할 필요는 없다.
- 실제 API 재검증에서 `회의는 짧은 게 좋은가?`는 1발언/8초 종료 대신 4~7발언으로 서로 다른 기준을 꺼낸 뒤 종료했다. 1분 메뉴 선택은 4발언에서 자연스럽게 결정됐다. 열린 주말 대화는 17발언까지 이어졌고, 후반의 유사 확인 발언은 계속 관찰할 품질 한계다.
- 2026-09-13 반응형 좌석 보정: 900px 이하 캐러셀에서 발화자는 하단 34px, 주변 인물은 10px가 테이블 뒤로 들어가도록 배치한다. 확대된 발화자가 테이블 선 위에 떠 보이던 3px 기준은 폐기했다.
- 반응 아이콘은 PC 48px, 900px 이하 42px이며 캐릭터 오른쪽 중앙에 둔다. 활성 좌석을 같은 줄의 다른 캐릭터보다 위에 그려 아이콘이 옆 캐릭터에 가려지지 않게 하고, 말풍선은 별도 상단 영역을 유지한다.

