# AI회의실 v5 대화 품질 점검

## 작업 분리

- 기준본: `C:\Users\kme69\Desktop\ai-make\ai-meeting`
- 개발본: `C:\Users\kme69\Desktop\ai-make\ai-meeting-next-20260912-051241`
- 롤백본: `C:\Users\kme69\Desktop\ai-make\ai-meeting-snapshots\ai-meeting-before-autonomous-20260912-051241.zip`
- 무결성 목록: `C:\Users\kme69\Desktop\ai-make\ai-meeting-snapshots\ai-meeting-before-autonomous-20260912-051241.sha256.txt`

UI 파일은 변경하지 않았다.

## 변경 전 관찰

- 메뉴 선택에서 여러 캐릭터가 같은 맵기·부담 조건을 반복했다.
- 일상 대화가 카페, 산책, 영상, 멍때리기 같은 안전한 평균 답으로 모였다.
- 사용자의 결혼식 일정을 AI가 자기 일정처럼 말했고, 열린 질문을 외면하기도 했다.
- 주 4일제는 조건 실패 시 반대만 정했는데도 결정 완료로 판정됐다.

## v5 후보

- `character_meeting_v4.py`를 보존하고 새 `character_meeting_v5.py`를 adapter에 연결했다.
- 독립 초기 렌즈에 고정 취미가 아닌 느슨한 생활 단서와 선택적인 `quirk`를 추가했다.
- 다른 참석자의 private state는 화자 선택에만 쓰며, 첫 발언은 주제 자체에서 시작한다.
- 화자는 최근 자기 발언 3개와 이번에 새로 더할 내용을 함께 본다.
- 사용자 발언 직후에만 질문 무시, 개인 사실 도용, 무관한 자기 반복을 최소 수정하는 재검토를 한다.
- 한쪽 조건만 정한 상태는 완전한 조건부 결정으로 인정하지 않는다.

## 실제 모델 재실행

- 1분 메뉴: 짜장을 고른 개발자와 짬뽕을 고른 CEO가 갈렸고 짬뽕으로 결정됐다. 후반 유사 발언 한 번은 남았다.
- 1분 퇴근 후: 짧은 목적 외출, 영상 하나만 보기, 작은 개인 의식 등 서로 다른 장면이 나왔고 `discussed`로 끝났다.
- 3분 주말 참여: 즉흥 맛집 외출, 서랍 한 칸 정리, 사용자의 결혼식·개인 프로젝트가 나왔다. 여기서 사실 도용을 재현한 뒤 사용자 전용 재검토를 추가했다.
- 3분 디자인 갈등: 사용자의 페이지 분리 제안에 클라이언트가 직접 반응했고 구체적인 해결로 이어졌다.
- 5분 두 사례는 OpenAI 계정의 `429 insufficient_quota` 때문에 변경 후 실행을 시작하지 못했다.

## 검증

- 엔진·adapter 계약 검사 28개 통과.
- health, 입력 검증, 독립 session, 취소, subprocess, SSE 발화·타이머·서기·요약·종료 통합 검사 통과.
- Python 구문 검사 통과.
- `static/`, `templates/`는 기준본과 바이트 단위로 같다.

API 잔액이 준비되면 `tests/live_conversation_eval.py`로 `launch_budget`, `four_day_week`, `weekend_with_human`을 다시 실행한다. 사용자 전용 재검토가 사실 소유권을 지키면서 말투를 평평하게 만들지 않는지가 핵심 관찰점이다.

## 짧은 종료와 후반 반복의 동시 보정

- 시간이나 최소 발언 수를 채우게 하지 않는다. 1/3/5분은 탐색 깊이와 안전 상한으로만 쓴다.
- 회의 전 private casting이 각 캐릭터에게 서로 공개되지 않는 출발점 하나를 준 뒤, 기존 independent lens를 각자 따로 생성한다. 찬반 배정이나 전원 발언 지시는 없다.
- controller는 CONTINUE마다 아직 나오지 않은 구체적인 차이를 지정한다. 이미 나온 말의 요약·동의·표현 변경뿐이면 END한다.
- 실제 API `short_meeting` 재실행은 4발언과 7발언에서 각각 자연 종료했다. 이전의 1발언 종료는 재현되지 않았고, 안전 최대 턴도 채우지 않았다.
- 실제 API `quick_choice`는 4발언에서 짬뽕으로 결정되어, 단순 선택을 불필요하게 늘이지 않았다.
- 실제 API `weekend_with_human`은 개인 장면과 사용자 입력을 따라 17발언까지 이어졌다. 후반에 유사한 확인이 남아 있어, 향후에는 새 내용이 없는 후보 발언을 표시 전에 폐기하는 의미 기반 검토를 고려한다. 이를 고정 발언 수나 문장 패턴 규칙으로 구현하지 않는다.
